<header class="navbar navbar-dark bg-dark">
    <div class="container">
        <%= link_to "Home", root_path %>
        <nav>
            <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                <li>
                    <%= link_to "La page du club !", home_path %>
                </li>
                <li class="divider"></li>
                <li>
                <a href="/users/<%=current_user.id%>">Ton profil</a>
                </li>
                <li>
                <%= link_to "Log out", logout_path, method: :delete %>
                </li>
                <% else %>
                <li>
                    <%= link_to "Enregistre-toi !", login_path %>
                </li>
                <li>
                    <%= link_to "Inscris-toi !", signup_path %>
                </li>
                <% end %>
            </ul>
        </nav>
    </div>
</header>